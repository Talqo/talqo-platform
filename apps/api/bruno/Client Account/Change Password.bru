meta {
  name: Change Password
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/client/me/password
  body: json
  auth: bearer
}

auth:bearer {
  token: {{clientToken}}
}

body:json {
  {
    "currentPassword": "oldpassword123",
    "newPassword": "newpassword123"
  }
}
