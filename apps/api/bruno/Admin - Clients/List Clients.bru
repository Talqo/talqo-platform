meta {
  name: List Clients
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/admin/clients
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

params:query {
  ~limit: 20
  ~offset: 0
}
