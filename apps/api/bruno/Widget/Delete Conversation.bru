meta {
  name: Delete Conversation
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/widget/{{clientId}}/sessions/{{sessionId}}/conversations/{{conversationId}}
  body: none
  auth: none
}

headers {
  X-Widget-Token: {{widgetToken}}
}
