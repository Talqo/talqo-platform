<overview>
Standalone, publishable React component library. Built with Vite in library mode.
</overview>
<conventions>
- This is the only **public** (publishable) package in the monorepo.
- Peer dependencies: `react ^18 || ^19`, `react-dom ^18 || ^19` — do not bundle React.
</conventions>
