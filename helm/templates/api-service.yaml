apiVersion: v1
kind: Service
metadata:
  name: {{ include "talqo.fullname" . }}-api
  labels:
    {{- include "talqo.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.api.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "talqo.api.selectorLabels" . | nindent 4 }}
