apiVersion: v1
kind: Service
metadata:
  name: {{ include "pagepal.fullname" . }}-api
  labels:
    {{- include "pagepal.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.api.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pagepal.api.selectorLabels" . | nindent 4 }}
