apiVersion: v1
kind: Service
metadata:
  name: {{ include "pagepal.fullname" . }}-web
  labels:
    {{- include "pagepal.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.web.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pagepal.web.selectorLabels" . | nindent 4 }}
