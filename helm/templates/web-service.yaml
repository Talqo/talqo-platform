apiVersion: v1
kind: Service
metadata:
  name: {{ include "talqo.fullname" . }}-web
  labels:
    {{- include "talqo.labels" . | nindent 4 }}
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.web.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "talqo.web.selectorLabels" . | nindent 4 }}
